@model IEnumerable<FireV2.Models.DocTypeAthelet>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.Athlete.Fnam)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.InfoDocumentTitleType.InfoDocTitleType)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.Athlete.Fnam)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.InfoDocumentTitleType.InfoDocTitleType)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.DocTypeAtId }) |
            @Html.ActionLink("Details", "Details", new { id=item.DocTypeAtId }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.DocTypeAtId })
        </td>
    </tr>
}

</table>
